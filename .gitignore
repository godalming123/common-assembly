# This is the binary that the `go build` command produces
main

# This is the prefix for files that the common assembly compiler produces
out*

# Files that end in _string are generated by stringer
# (docs: https://pkg.go.dev/golang.org/x/tools/cmd/stringer)
*_string.go
